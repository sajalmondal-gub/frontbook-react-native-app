import { View, Text } from 'react-native';

export default function ReelsScreen() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text className="text-xl font-bold">Reels</Text>
        </View>
    );
}
